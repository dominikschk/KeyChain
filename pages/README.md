# Seiten (Routing)

- **/** oder **/configurator** – Konfigurator (NFeC konfigurieren, bestellen)
- **/ccp** – Customer Control Panel (Scans; Bearbeiten nur mit `?t=` Write-Token aus `_CCP-URL`)
- **/admin** – Admin (alle Bestellungen mit Microsite-URL-Links)

Router: App.tsx
