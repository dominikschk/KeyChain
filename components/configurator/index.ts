/**
 * Konfigurator – Komponenten nur für den Konfigurator (3D + Microsite-Editor).
 * Re-Export aus dem gemeinsamen components/ für klare Zuordnung.
 */
export { Controls } from '../Controls';
export { Viewer } from '../Viewer';
