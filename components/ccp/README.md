# CCP – Customer Control Panel

Komponenten für das **Kunden-Panel** (nach Bestellung):

- **Microsite-Link + Scans** – immer mit `?id=SHORT_ID`
- **Microsite bearbeiten** – nur mit Write-Token: `/ccp?id=SHORT_ID&t=WRITE_TOKEN` (aus Shopify `_CCP-URL`)

Seite: `/ccp` (pages/CcpPage.tsx)
