# ============================================================
# Umgebungsvariablen – Kopie nach .env.local (lokal) oder in
# Vercel/Netlify → Settings → Environment Variables (Deploy).
# ============================================================

# ---- Pflicht für Konfigurator, Microsite, CCP, Admin ----
# Supabase: Project Settings → API → Project URL und anon (public) key
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---- Admin-Panel (stark empfohlen vor Deploy) ----
# Ohne VITE_ADMIN_PASSWORD ist /admin gesperrt (niemand kommt rein).
# Mit Passwort: Login, dann Session 8 Std., nach 5 Fehlversuchen 15 Min. Lockout.
VITE_ADMIN_PASSWORD=

# Optional: Session-Dauer in Stunden (Standard 8)
# VITE_ADMIN_SESSION_HOURS=8

# ---- Optional: Google-Login im Konfigurator ----
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
VITE_GOOGLE_CLIENT_ID=
